uniform half4 colorWhite;

void main() {
    const int x = 1;
    @switch (x) {
        case 1:
            sk_FragColor = half4(1);
            if (colorWhite.x == colorWhite.y) break;
        default:
            sk_FragColor = half4(0);
    }
}
