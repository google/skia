uniform half4 colorGreen, colorRed;
uniform half2x2 testMatrix2x2;

half4 main(float2 coords) {
    return half4(testMatrix2x2) == half4(1, 2, 3, 4)
                ? colorGreen
                : colorRed;
}
