void main() {
    float x;
    switch (int(sqrt(2))) {
        case 0:
            x = 0.0;
        case 1:
            x = 1.0;
        default:
            x = 2.0;
    }
    sk_FragColor = half4(half(x));
}
