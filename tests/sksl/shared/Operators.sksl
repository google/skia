void main() {
    float x = 1, y = 2;
    int z = 3;
    x = x - x + y * z * x * (y - z);
    y = x / y / z;
    z = (z / 2 % 3 << 4) >> 2 << 1;
    bool b = (x > 4) == x < 2 || 2 >= sqrt(2) && y <= z;
    x += 12;
    x -= 12;
    x *= y /= z = 10;
    b ||= false;
    b &&= true;
    b ^^= false;
    z |= 0;
    z &= -1;
    z ^= 0;
    z >>= 2;
    z <<= 4;
    z %= 5;
    x = (float2(sqrt(1)), 6);
    z = (float2(sqrt(1)), 6);
}
