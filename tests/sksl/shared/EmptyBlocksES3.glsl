
out vec4 sk_FragColor;
vec4 main() {
    vec4 color = vec4(0.0);
    for (int counter = 0;counter < 10; ++counter) int x;
    for (int counter = 0;counter < 10; ++counter) int y;
    int z;

    for (int counter = 0;counter < 10; ++counter) true;
    if (sqrt(1.0) == 1.0) color.y = 1.0; else 1;
    if (sqrt(1.0) == 2.0) 0; else color.w = 1.0;
    if (sqrt(1.0) == 3.0) 0; else 1;
    while (sqrt(1.0) == 2.0) 6;
    do 6; while (sqrt(1.0) == 2.0);
    return color;
}
