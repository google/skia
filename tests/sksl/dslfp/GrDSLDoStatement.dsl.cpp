

/**************************************************************************************************
 *** This file was autogenerated from GrDSLDoStatement.fp; do not modify.
 **************************************************************************************************/
/* TODO(skia:11854): DSLCPPCodeGenerator is currently a work in progress. */
#include "GrDSLDoStatement.h"

#include "src/core/SkUtils.h"
#include "src/gpu/GrTexture.h"
#include "src/gpu/glsl/GrGLSLFragmentProcessor.h"
#include "src/gpu/glsl/GrGLSLFragmentShaderBuilder.h"
#include "src/gpu/glsl/GrGLSLProgramBuilder.h"
#include "src/sksl/SkSLCPP.h"
#include "src/sksl/SkSLUtil.h"
class GrGLSLDSLDoStatement : public GrGLSLFragmentProcessor {
public:
    GrGLSLDSLDoStatement() {}
    void emitCode(EmitArgs& args) override {
        const GrDSLDoStatement& _outer = args.fFp.cast<GrDSLDoStatement>();
        (void) _outer;

        using namespace SkSL::dsl;
        StartFragmentProcessor(this, &args);
Var color(DSLModifiers(), DSLType(kHalf4_Type), "color", Half4(1.0, 1.0, 1.0, 1.0));
Declare(color);
Do(color.x() -= 0.25, /*While:*/ color.x() > 0.5);
Do(Block(color.x() -= 0.25, If(color.x() <= 0.0, /*Then:*/ Break())), /*While:*/ color.w() == 1.0);
Do(Block(color.z() -= 0.25, If(color.w() == 1.0, /*Then:*/ Continue()), color.y() = 0.0), /*While:*/ color.z() > 0.0);
Return(color);
        EndFragmentProcessor();
    }
private:
    void onSetData(const GrGLSLProgramDataManager& pdman, const GrFragmentProcessor& _proc) override {
    }
};
std::unique_ptr<GrGLSLFragmentProcessor> GrDSLDoStatement::onMakeProgramImpl() const {
    return std::make_unique<GrGLSLDSLDoStatement>();
}
void GrDSLDoStatement::onGetGLSLProcessorKey(const GrShaderCaps& caps, GrProcessorKeyBuilder* b) const {
}
bool GrDSLDoStatement::onIsEqual(const GrFragmentProcessor& other) const {
    const GrDSLDoStatement& that = other.cast<GrDSLDoStatement>();
    (void) that;
    return true;
}
GrDSLDoStatement::GrDSLDoStatement(const GrDSLDoStatement& src)
: INHERITED(kGrDSLDoStatement_ClassID, src.optimizationFlags()) {
        this->cloneAndRegisterAllChildProcessors(src);
}
std::unique_ptr<GrFragmentProcessor> GrDSLDoStatement::clone() const {
    return std::make_unique<GrDSLDoStatement>(*this);
}
#if GR_TEST_UTILS
SkString GrDSLDoStatement::onDumpInfo() const {
    return SkString();
}
#endif
