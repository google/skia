

/**************************************************************************************************
 *** This file was autogenerated from GrDSLTestForStatement.fp; do not modify.
 **************************************************************************************************/
/* TODO(skia:11854): DSLCPPCodeGenerator is currently a work in progress. */
#include "GrDSLTestForStatement.h"

#include "src/core/SkUtils.h"
#include "src/gpu/GrTexture.h"
#include "src/gpu/glsl/GrGLSLFragmentProcessor.h"
#include "src/gpu/glsl/GrGLSLFragmentShaderBuilder.h"
#include "src/gpu/glsl/GrGLSLProgramBuilder.h"
#include "src/sksl/SkSLCPP.h"
#include "src/sksl/SkSLUtil.h"
#include "src/sksl/dsl/priv/DSLFPs.h"
#include "src/sksl/dsl/priv/DSLWriter.h"

#if defined(__clang__)
#pragma clang diagnostic ignored "-Wcomma"
#endif

class GrGLSLDSLTestForStatement : public GrGLSLFragmentProcessor {
public:
    GrGLSLDSLTestForStatement() {}
    void emitCode(EmitArgs& args) override {
        const GrDSLTestForStatement& _outer = args.fFp.cast<GrDSLTestForStatement>();
        (void) _outer;

        using namespace SkSL::dsl;
        StartFragmentProcessor(this, &args);
Var color(DSLModifiers(), DSLType(kHalf4_Type), "color", Half4(1.0));
Var a(DSLModifiers(), DSLType(kHalf_Type), "a", 0.0);
Var r(DSLModifiers(), DSLType(kHalf_Type), "r", -5.0);
Var b(DSLModifiers(), DSLType(kHalf_Type), "b", 5.0);
Var x(DSLModifiers(), DSLType(kHalf_Type), "x", 0.0);
Var y(DSLModifiers(), DSLType(kHalf_Type), "y", 1.0);
Declare(color);
For(Declare(a), a <= 1.0, ++a, /*Body:*/ color.w() = a);
For(Declare(r), r < 5.0, r += 1.0, /*Body:*/ Block(color.x() = r, If(color.x() == 0.0, /*Then:*/ Break())));
For(Declare(b), b >= 0.0, b -= 1.0, /*Body:*/ Block(color.z() = b, If(color.w() == 1.0, /*Then:*/ Continue()), color.y() = 0.0));
For((Declare(x), Declare(y)), x <= y, ++x, /*Body:*/ Block(color.w() = x));
Return(color);
        EndFragmentProcessor();
    }
private:
    void onSetData(const GrGLSLProgramDataManager& pdman, const GrFragmentProcessor& _proc) override {
    }
};
std::unique_ptr<GrGLSLFragmentProcessor> GrDSLTestForStatement::onMakeProgramImpl() const {
    return std::make_unique<GrGLSLDSLTestForStatement>();
}
void GrDSLTestForStatement::onGetGLSLProcessorKey(const GrShaderCaps& caps, GrProcessorKeyBuilder* b) const {
}
bool GrDSLTestForStatement::onIsEqual(const GrFragmentProcessor& other) const {
    const GrDSLTestForStatement& that = other.cast<GrDSLTestForStatement>();
    (void) that;
    return true;
}
GrDSLTestForStatement::GrDSLTestForStatement(const GrDSLTestForStatement& src)
: INHERITED(kGrDSLTestForStatement_ClassID, src.optimizationFlags()) {
        this->cloneAndRegisterAllChildProcessors(src);
}
std::unique_ptr<GrFragmentProcessor> GrDSLTestForStatement::clone() const {
    return std::make_unique<GrDSLTestForStatement>(*this);
}
#if GR_TEST_UTILS
SkString GrDSLTestForStatement::onDumpInfo() const {
    return SkString();
}
#endif
