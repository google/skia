

/**************************************************************************************************
 *** This file was autogenerated from GrDSLTestDoStatement.fp; do not modify.
 **************************************************************************************************/
/* TODO(skia:11854): DSLCPPCodeGenerator is currently a work in progress. */
#include "GrDSLTestDoStatement.h"

#include "src/core/SkUtils.h"
#include "src/gpu/GrTexture.h"
#include "src/gpu/glsl/GrGLSLFragmentProcessor.h"
#include "src/gpu/glsl/GrGLSLFragmentShaderBuilder.h"
#include "src/gpu/glsl/GrGLSLProgramBuilder.h"
#include "src/sksl/SkSLCPP.h"
#include "src/sksl/SkSLUtil.h"
#include "src/sksl/dsl/priv/DSLFPs.h"
#include "src/sksl/dsl/priv/DSLWriter.h"

#if defined(__clang__)
#pragma clang diagnostic ignored "-Wcomma"
#endif

class GrGLSLDSLTestDoStatement : public GrGLSLFragmentProcessor {
public:
    GrGLSLDSLTestDoStatement() {}
    void emitCode(EmitArgs& args) override {
        const GrDSLTestDoStatement& _outer = args.fFp.cast<GrDSLTestDoStatement>();
        (void) _outer;

        using namespace SkSL::dsl;
        StartFragmentProcessor(this, &args);
Var color(DSLModifiers(), DSLType(kHalf4_Type), "color", Half4(1.0, 1.0, 1.0, 1.0));
Declare(color);
Do(color.x() -= 0.25, /*While:*/ color.x() > 0.5);
Do(Block(color.x() -= 0.25, If(color.x() <= 0.0, /*Then:*/ Break())), /*While:*/ color.w() == 1.0);
Do(Block(color.z() -= 0.25, If(color.w() == 1.0, /*Then:*/ Continue()), color.y() = 0.0), /*While:*/ color.z() > 0.0);
Return(color);
        EndFragmentProcessor();
    }
private:
    void onSetData(const GrGLSLProgramDataManager& pdman, const GrFragmentProcessor& _proc) override {
    }
};
std::unique_ptr<GrGLSLFragmentProcessor> GrDSLTestDoStatement::onMakeProgramImpl() const {
    return std::make_unique<GrGLSLDSLTestDoStatement>();
}
void GrDSLTestDoStatement::onGetGLSLProcessorKey(const GrShaderCaps& caps, GrProcessorKeyBuilder* b) const {
}
bool GrDSLTestDoStatement::onIsEqual(const GrFragmentProcessor& other) const {
    const GrDSLTestDoStatement& that = other.cast<GrDSLTestDoStatement>();
    (void) that;
    return true;
}
GrDSLTestDoStatement::GrDSLTestDoStatement(const GrDSLTestDoStatement& src)
: INHERITED(kGrDSLTestDoStatement_ClassID, src.optimizationFlags()) {
        this->cloneAndRegisterAllChildProcessors(src);
}
std::unique_ptr<GrFragmentProcessor> GrDSLTestDoStatement::clone() const {
    return std::make_unique<GrDSLTestDoStatement>(*this);
}
#if GR_TEST_UTILS
SkString GrDSLTestDoStatement::onDumpInfo() const {
    return SkString();
}
#endif
