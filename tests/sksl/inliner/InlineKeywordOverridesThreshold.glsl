
void main() {
    int y = 0;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;

    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;
    ++y;

}
