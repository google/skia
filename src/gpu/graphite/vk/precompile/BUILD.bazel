load("//bazel:skia_rules.bzl", "skia_filegroup")

package(
    default_applicable_licenses = ["//:license"],
)

licenses(["notice"])

# Filegroup for gni exporting
skia_filegroup(
    name = "vk_precompile_srcs",
    srcs = [
        "VulkanPrecompileShader.cpp",
    ],
)
