/*
 * Copyright 2018 Google Inc.
 *
 * Use of this source code is governed by a BSD-style license that can be
 * found in the LICENSE file.
 */

/**************************************************************************************************
 *** This file was autogenerated from GrDualIntervalGradientColorizer.fp; do not modify.
 **************************************************************************************************/
#include "GrDualIntervalGradientColorizer.h"
#include "glsl/GrGLSLFragmentProcessor.h"
#include "glsl/GrGLSLFragmentShaderBuilder.h"
#include "glsl/GrGLSLProgramBuilder.h"
#include "GrTexture.h"
#include "SkSLCPP.h"
#include "SkSLUtil.h"
class GrGLSLDualIntervalGradientColorizer : public GrGLSLFragmentProcessor {
public:
    GrGLSLDualIntervalGradientColorizer() {}
    void emitCode(EmitArgs& args) override {
        GrGLSLFPFragmentBuilder* fragBuilder = args.fFragBuilder;
        const GrDualIntervalGradientColorizer& _outer =
                args.fFp.cast<GrDualIntervalGradientColorizer>();
        (void)_outer;
        auto c0 = _outer.c0();
        (void)c0;
        auto c1 = _outer.c1();
        (void)c1;
        auto c2 = _outer.c2();
        (void)c2;
        auto c3 = _outer.c3();
        (void)c3;
        auto threshold = _outer.threshold();
        (void)threshold;
        fUScale01Var = args.fUniformHandler->addUniform(kFragment_GrShaderFlag, kHalf4_GrSLType,
                                                        kDefault_GrSLPrecision, "uScale01");
        fUBias01Var = args.fUniformHandler->addUniform(kFragment_GrShaderFlag, kHalf4_GrSLType,
                                                       kDefault_GrSLPrecision, "uBias01");
        fUScale23Var = args.fUniformHandler->addUniform(kFragment_GrShaderFlag, kHalf4_GrSLType,
                                                        kDefault_GrSLPrecision, "uScale23");
        fUBias23Var = args.fUniformHandler->addUniform(kFragment_GrShaderFlag, kHalf4_GrSLType,
                                                       kDefault_GrSLPrecision, "uBias23");
        fUThresholdVar = args.fUniformHandler->addUniform(kFragment_GrShaderFlag, kFloat_GrSLType,
                                                          kDefault_GrSLPrecision, "uThreshold");
        fragBuilder->codeAppendf(
                "half t = %s.x;\nhalf4 scale, bias;\nif (float(t) < %s) {\n    scale = %s;\n    "
                "bias = %s;\n} else {\n    scale = %s;\n    bias = %s;\n}\n%s = t * scale + "
                "bias;\n",
                args.fInputColor, args.fUniformHandler->getUniformCStr(fUThresholdVar),
                args.fUniformHandler->getUniformCStr(fUScale01Var),
                args.fUniformHandler->getUniformCStr(fUBias01Var),
                args.fUniformHandler->getUniformCStr(fUScale23Var),
                args.fUniformHandler->getUniformCStr(fUBias23Var), args.fOutputColor);
    }

private:
    void onSetData(const GrGLSLProgramDataManager& pdman,
                   const GrFragmentProcessor& _proc) override {
        const GrDualIntervalGradientColorizer& _outer =
                _proc.cast<GrDualIntervalGradientColorizer>();
        auto c0 = _outer.c0();
        (void)c0;
        auto c1 = _outer.c1();
        (void)c1;
        auto c2 = _outer.c2();
        (void)c2;
        auto c3 = _outer.c3();
        (void)c3;
        UniformHandle& uScale01 = fUScale01Var;
        (void)uScale01;
        UniformHandle& uBias01 = fUBias01Var;
        (void)uBias01;
        UniformHandle& uScale23 = fUScale23Var;
        (void)uScale23;
        UniformHandle& uBias23 = fUBias23Var;
        (void)uBias23;
        auto threshold = _outer.threshold();
        (void)threshold;
        UniformHandle& uThreshold = fUThresholdVar;
        (void)uThreshold;

        // Transform the colors before loading to the GPU so that the shader code is a simple
        // scale+bias
        auto vc0 = Sk4f::Load(c0.fRGBA);
        auto vc1 = Sk4f::Load(c1.fRGBA);
        auto scale = (vc1 - vc0) / threshold;
        pdman.set4f(uBias01, vc0[0], vc0[1], vc0[2], vc0[3]);
        pdman.set4f(uScale01, scale[0], scale[1], scale[2], scale[3]);

        auto vc2 = Sk4f::Load(c2.fRGBA);
        auto vc3 = Sk4f::Load(c3.fRGBA);
        scale = (vc3 - vc2) / (1 - threshold);
        auto bias = vc2 - threshold * scale;
        pdman.set4f(uBias23, bias[0], bias[1], bias[2], bias[3]);
        pdman.set4f(uScale23, scale[0], scale[1], scale[2], scale[3]);

        pdman.set1f(uThreshold, threshold);
    }
    UniformHandle fUScale01Var;
    UniformHandle fUBias01Var;
    UniformHandle fUScale23Var;
    UniformHandle fUBias23Var;
    UniformHandle fUThresholdVar;
};
GrGLSLFragmentProcessor* GrDualIntervalGradientColorizer::onCreateGLSLInstance() const {
    return new GrGLSLDualIntervalGradientColorizer();
}
void GrDualIntervalGradientColorizer::onGetGLSLProcessorKey(const GrShaderCaps& caps,
                                                            GrProcessorKeyBuilder* b) const {}
bool GrDualIntervalGradientColorizer::onIsEqual(const GrFragmentProcessor& other) const {
    const GrDualIntervalGradientColorizer& that = other.cast<GrDualIntervalGradientColorizer>();
    (void)that;
    if (fC0 != that.fC0) return false;
    if (fC1 != that.fC1) return false;
    if (fC2 != that.fC2) return false;
    if (fC3 != that.fC3) return false;
    if (fThreshold != that.fThreshold) return false;
    return true;
}
GrDualIntervalGradientColorizer::GrDualIntervalGradientColorizer(
        const GrDualIntervalGradientColorizer& src)
        : INHERITED(kGrDualIntervalGradientColorizer_ClassID, src.optimizationFlags())
        , fC0(src.fC0)
        , fC1(src.fC1)
        , fC2(src.fC2)
        , fC3(src.fC3)
        , fThreshold(src.fThreshold) {}
std::unique_ptr<GrFragmentProcessor> GrDualIntervalGradientColorizer::clone() const {
    return std::unique_ptr<GrFragmentProcessor>(new GrDualIntervalGradientColorizer(*this));
}
