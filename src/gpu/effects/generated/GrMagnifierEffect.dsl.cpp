/*
 * Copyright 2018 Google Inc.
 *
 * Use of this source code is governed by a BSD-style license that can be
 * found in the LICENSE file.
 */

/**************************************************************************************************
 *** This file was autogenerated from GrMagnifierEffect.fp; do not modify.
 **************************************************************************************************/
/* TODO(skia:11854): DSLCPPCodeGenerator is currently a work in progress. */
#include "GrMagnifierEffect.h"

#include "src/core/SkUtils.h"
#include "src/gpu/GrTexture.h"
#include "src/gpu/glsl/GrGLSLFragmentProcessor.h"
#include "src/gpu/glsl/GrGLSLFragmentShaderBuilder.h"
#include "src/gpu/glsl/GrGLSLProgramBuilder.h"
#include "src/sksl/SkSLCPP.h"
#include "src/sksl/SkSLUtil.h"
#include "src/sksl/dsl/priv/DSLFPs.h"
#include "src/sksl/dsl/priv/DSLWriter.h"

class GrGLSLMagnifierEffect : public GrGLSLFragmentProcessor {
public:
    GrGLSLMagnifierEffect() {}
    void emitCode(EmitArgs& args) override {
        [[maybe_unused]] const GrMagnifierEffect& _outer = args.fFp.cast<GrMagnifierEffect>();

        using namespace SkSL::dsl;
        StartFragmentProcessor(this, &args);
        [[maybe_unused]] const auto& bounds = _outer.bounds;
        Var _bounds(kConst_Modifier,
                    DSLType(kInt4_Type),
                    "bounds",
                    Int4(bounds.left(), bounds.top(), bounds.right(), bounds.bottom()));
        Declare(_bounds);
        [[maybe_unused]] const auto& srcRect = _outer.srcRect;
        Var _srcRect(kConst_Modifier,
                     DSLType(kFloat4_Type),
                     "srcRect",
                     Float4(srcRect.left(), srcRect.top(), srcRect.right(), srcRect.bottom()));
        Declare(_srcRect);
        Var _boundsUniform(kUniform_Modifier, DSLType(kFloat4_Type), "boundsUniform");
        boundsUniformVar = VarUniformHandle(_boundsUniform);
        DeclareGlobal(_boundsUniform);
        Var _xInvZoom(kUniform_Modifier, DSLType(kFloat_Type), "xInvZoom");
        xInvZoomVar = VarUniformHandle(_xInvZoom);
        DeclareGlobal(_xInvZoom);
        Var _yInvZoom(kUniform_Modifier, DSLType(kFloat_Type), "yInvZoom");
        yInvZoomVar = VarUniformHandle(_yInvZoom);
        DeclareGlobal(_yInvZoom);
        Var _xInvInset(kUniform_Modifier, DSLType(kFloat_Type), "xInvInset");
        xInvInsetVar = VarUniformHandle(_xInvInset);
        DeclareGlobal(_xInvInset);
        Var _yInvInset(kUniform_Modifier, DSLType(kFloat_Type), "yInvInset");
        yInvInsetVar = VarUniformHandle(_yInvInset);
        DeclareGlobal(_yInvInset);
        Var _offset(kUniform_Modifier, DSLType(kHalf2_Type), "offset");
        offsetVar = VarUniformHandle(_offset);
        DeclareGlobal(_offset);
        Var _zoom_coord(kNo_Modifier,
                        DSLType(kFloat2_Type),
                        "zoom_coord",
                        Float2(_offset) + sk_SampleCoord() * Float2(_xInvZoom, _yInvZoom));
        Var _delta(
                kNo_Modifier,
                DSLType(kFloat2_Type),
                "delta",
                (sk_SampleCoord() - Swizzle(_boundsUniform, X, Y)) * Swizzle(_boundsUniform, Z, W));
        Var _weight(kNo_Modifier, DSLType(kFloat_Type), "weight", 0.0f);
        Var _dist(kNo_Modifier, DSLType(kFloat_Type), "dist", Length(_delta));
        Var _delta_squared(kNo_Modifier, DSLType(kFloat2_Type), "delta_squared", _delta * _delta);
        Declare(_zoom_coord);
        Declare(_delta);
        _delta = Min(_delta, Float2(1.0f) - _delta);
        _delta *= Float2(_xInvInset, _yInvInset);
        Declare(_weight);
        If(_delta.x() < 2.0f && _delta.y() < 2.0f,
           /*Then:*/
           Block(_delta = Float2(2.0f) - _delta,
                 Declare(_dist),
                 _dist = Max(2.0f - _dist, 0.0f),
                 _weight = Min(_dist * _dist, 1.0f)),
           /*Else:*/
           Block(Declare(_delta_squared),
                 _weight = Min(Min(_delta_squared.x(), _delta_squared.y()), 1.0f)));
        Return(SampleChild(0, Mix(sk_SampleCoord(), _zoom_coord, _weight)));
        EndFragmentProcessor();
    }

private:
    void onSetData(const GrGLSLProgramDataManager& pdman,
                   const GrFragmentProcessor& _proc) override {
        const GrMagnifierEffect& _outer = _proc.cast<GrMagnifierEffect>();
        {
            pdman.set1f(xInvZoomVar, (_outer.xInvZoom));
            pdman.set1f(yInvZoomVar, (_outer.yInvZoom));
            pdman.set1f(xInvInsetVar, (_outer.xInvInset));
            pdman.set1f(yInvInsetVar, (_outer.yInvInset));
        }
        [[maybe_unused]] const auto& bounds = _outer.bounds;
        [[maybe_unused]] UniformHandle& boundsUniform = boundsUniformVar;
        [[maybe_unused]] const auto& srcRect = _outer.srcRect;
        [[maybe_unused]] UniformHandle& xInvZoom = xInvZoomVar;
        [[maybe_unused]] UniformHandle& yInvZoom = yInvZoomVar;
        [[maybe_unused]] UniformHandle& xInvInset = xInvInsetVar;
        [[maybe_unused]] UniformHandle& yInvInset = yInvInsetVar;
        [[maybe_unused]] UniformHandle& offset = offsetVar;

        pdman.set2f(offset, srcRect.x(), srcRect.y());
        pdman.set4f(
                boundsUniform, bounds.x(), bounds.y(), 1.f / bounds.width(), 1.f / bounds.height());
    }
    UniformHandle boundsUniformVar;
    UniformHandle offsetVar;
    UniformHandle xInvZoomVar;
    UniformHandle yInvZoomVar;
    UniformHandle xInvInsetVar;
    UniformHandle yInvInsetVar;
};
std::unique_ptr<GrGLSLFragmentProcessor> GrMagnifierEffect::onMakeProgramImpl() const {
    return std::make_unique<GrGLSLMagnifierEffect>();
}
void GrMagnifierEffect::onGetGLSLProcessorKey(const GrShaderCaps& caps,
                                              GrProcessorKeyBuilder* b) const {}
bool GrMagnifierEffect::onIsEqual(const GrFragmentProcessor& other) const {
    const GrMagnifierEffect& that = other.cast<GrMagnifierEffect>();
    (void)that;
    if (bounds != that.bounds) return false;
    if (srcRect != that.srcRect) return false;
    if (xInvZoom != that.xInvZoom) return false;
    if (yInvZoom != that.yInvZoom) return false;
    if (xInvInset != that.xInvInset) return false;
    if (yInvInset != that.yInvInset) return false;
    return true;
}
GrMagnifierEffect::GrMagnifierEffect(const GrMagnifierEffect& src)
        : INHERITED(kGrMagnifierEffect_ClassID, src.optimizationFlags())
        , bounds(src.bounds)
        , srcRect(src.srcRect)
        , xInvZoom(src.xInvZoom)
        , yInvZoom(src.yInvZoom)
        , xInvInset(src.xInvInset)
        , yInvInset(src.yInvInset) {
    this->cloneAndRegisterAllChildProcessors(src);
    this->setUsesSampleCoordsDirectly();
}
std::unique_ptr<GrFragmentProcessor> GrMagnifierEffect::clone() const {
    return std::make_unique<GrMagnifierEffect>(*this);
}
#if GR_TEST_UTILS
SkString GrMagnifierEffect::onDumpInfo() const {
    return SkStringPrintf(
            "(bounds=int4(%d, %d, %d, %d), srcRect=float4(%f, %f, %f, %f), xInvZoom=%f, "
            "yInvZoom=%f, xInvInset=%f, yInvInset=%f)",
            bounds.left(),
            bounds.top(),
            bounds.right(),
            bounds.bottom(),
            srcRect.left(),
            srcRect.top(),
            srcRect.right(),
            srcRect.bottom(),
            xInvZoom,
            yInvZoom,
            xInvInset,
            yInvInset);
}
#endif
GR_DEFINE_FRAGMENT_PROCESSOR_TEST(GrMagnifierEffect);
#if GR_TEST_UTILS
std::unique_ptr<GrFragmentProcessor> GrMagnifierEffect::TestCreate(GrProcessorTestData* d) {
    const int kMaxWidth = 200;
    const int kMaxHeight = 200;
    const SkScalar kMaxInset = 20.0f;
    uint32_t width = d->fRandom->nextULessThan(kMaxWidth);
    uint32_t height = d->fRandom->nextULessThan(kMaxHeight);
    SkScalar inset = d->fRandom->nextRangeScalar(1.0f, kMaxInset);

    SkIRect bounds = SkIRect::MakeWH(SkIntToScalar(kMaxWidth), SkIntToScalar(kMaxHeight));
    SkRect srcRect = SkRect::MakeWH(SkIntToScalar(width), SkIntToScalar(height));

    auto src = GrProcessorUnitTest::MakeChildFP(d);
    auto effect = GrMagnifierEffect::Make(std::move(src),
                                          bounds,
                                          srcRect,
                                          srcRect.width() / bounds.width(),
                                          srcRect.height() / bounds.height(),
                                          bounds.width() / inset,
                                          bounds.height() / inset);
    SkASSERT(effect);
    return effect;
}
#endif
