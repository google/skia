/*
 * Copyright 2017 Google Inc.
 *
 * Use of this source code is governed by a BSD-style license that can be
 * found in the LICENSE file.
 */

/*
 * This file was autogenerated from GrConvexPolyEffect.fp; do not modify.
 */
#include "GrConvexPolyEffect.h"
#if SK_SUPPORT_GPU
#include "glsl/GrGLSLFragmentProcessor.h"
#include "glsl/GrGLSLFragmentShaderBuilder.h"
#include "glsl/GrGLSLProgramBuilder.h"
#include "GrTexture.h"
#include "SkSLCPP.h"
#include "SkSLUtil.h"
class GrGLSLConvexPolyEffect : public GrGLSLFragmentProcessor {
public:
    GrGLSLConvexPolyEffect() { memset(prevEdges, 0, sizeof(prevEdges)); }
    void emitCode(EmitArgs& args) override {
        GrGLSLFPFragmentBuilder* fragBuilder = args.fFragBuilder;
        const GrConvexPolyEffect& _outer = args.fFp.cast<GrConvexPolyEffect>();
        (void)_outer;
        auto edgeType = _outer.edgeType();
        (void)edgeType;
        auto edgeCount = _outer.edgeCount();
        (void)edgeCount;
        auto inEdges = _outer.inEdges();
        (void)inEdges;
        fEdgesVar =
                args.fUniformHandler->addUniformArray(kFragment_GrShaderFlag, kHalf3_GrSLType,
                                                      kDefault_GrSLPrecision, "edges", edgeCount);
        fragBuilder->codeAppendf(
                "const int kMaxEdges = 8;\nhalf3 prevEdges[kMaxEdges * 3];\n%s = half4(float4(1.0, "
                "0.0, 0.0, 1.0));\n",
                args.fOutputColor);
    }

private:
    void onSetData(const GrGLSLProgramDataManager& pdman,
                   const GrFragmentProcessor& _proc) override {
        const GrConvexPolyEffect& _outer = _proc.cast<GrConvexPolyEffect>();
        auto edgeType = _outer.edgeType();
        (void)edgeType;
        auto edgeCount = _outer.edgeCount();
        (void)edgeCount;
        auto inEdges = _outer.inEdges();
        (void)inEdges;
        UniformHandle& edges = fEdgesVar;
        (void)edges;

        size_t byteSize = 3 * edgeCount * sizeof(SkScalar);
        if (memcmp(prevEdges, inEdges, byteSize)) {
            pdman.set3fv(edges, edgeCount, (SkScalar*)inEdges);
            memcpy(prevEdges, inEdges, byteSize);
        }
    }
    static constexpr int kMaxEdges = 8;
    SkVector3 prevEdges[(kMaxEdges * 3)];
    UniformHandle fEdgesVar;
};
GrGLSLFragmentProcessor* GrConvexPolyEffect::onCreateGLSLInstance() const {
    return new GrGLSLConvexPolyEffect();
}
void GrConvexPolyEffect::onGetGLSLProcessorKey(const GrShaderCaps& caps,
                                               GrProcessorKeyBuilder* b) const {
    b->add32((int32_t)fEdgeType);
    b->add32((int32_t)fEdgeCount);
}
bool GrConvexPolyEffect::onIsEqual(const GrFragmentProcessor& other) const {
    const GrConvexPolyEffect& that = other.cast<GrConvexPolyEffect>();
    (void)that;
    if (fEdgeType != that.fEdgeType) return false;
    if (fEdgeCount != that.fEdgeCount) return false;
    if (memcmp(fInEdges, that.fInEdges, sizeof(fInEdges))) return false;
    return true;
}
GrConvexPolyEffect::GrConvexPolyEffect(const GrConvexPolyEffect& src)
        : INHERITED(kGrConvexPolyEffect_ClassID, src.optimizationFlags())
        , fEdgeType(src.fEdgeType)
        , fEdgeCount(src.fEdgeCount) {
    memcpy(fInEdges, src.fInEdges, sizeof(fInEdges));
}
std::unique_ptr<GrFragmentProcessor> GrConvexPolyEffect::clone() const {
    return std::unique_ptr<GrFragmentProcessor>(new GrConvexPolyEffect(*this));
}
GR_DEFINE_FRAGMENT_PROCESSOR_TEST(GrConvexPolyEffect);
#if GR_TEST_UTILS
std::unique_ptr<GrFragmentProcessor> GrConvexPolyEffect::TestCreate(GrProcessorTestData* d) {
    int count = d->fRandom->nextULessThan(kMaxEdges) + 1;
    SkScalar edges[kMaxEdges * 3];
    for (int i = 0; i < 3 * count; ++i) {
        edges[i] = d->fRandom->nextSScalar1();
    }

    std::unique_ptr<GrFragmentProcessor> fp;
    do {
        GrClipEdgeType edgeType =
                static_cast<GrClipEdgeType>(d->fRandom->nextULessThan(kGrClipEdgeTypeCnt));
        fp = GrConvexPolyEffect::Make(edgeType, count, edges);
    } while (nullptr == fp);
    return fp;
}
#endif
#endif
