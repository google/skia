#!/usr/bin/env python
#
# Copyright 2016 Google Inc.
#
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

# Generate Android.bp for Skia from GN configuration.

import os
import pprint
import string
import sys

t = string.Template('''// This file is autogenerated by gn_to_bp.py.

cc_library {
  name: "libskia",
  cflags: [
    "-fexceptions",
    "-Wno-unused-parameter",
    "-Wvla",
    "-U_FORTIFY_SOURCE",
    "-D_FORTIFY_SOURCE=1",
    "-DSKIA_IMPLEMENTATION=1",
    "-DqDNGBigEndian=0",
  ],

  export_include_dirs: $export_includes,
  local_include_dirs: $local_includes,
  srcs: $srcs,

  arch: {
    arm: {
      srcs: $arm_srcs,
      armv7_a_neon: {
        srcs: $arm_neon_srcs,
      },
    },
    arm64: {
      srcs: $arm64_srcs,
    },

    mips: {
      srcs: $mips_srcs,
    }
    mips64: {
      srcs: $mips64_srcs,
    }

    x86: {
      srcs: $x86_srcs,
    },
    x86_64: {
      srcs: $x86_srcs,
    },
  }

  shared_libs: [
      "libEGL",
      "libGLESv2",
      "libdng_sdk",
      "libexpat",
      "libft2",
      "libicui18n",
      "libicuuc",
      "libjpeg",
      "liblog",
      "libpiex",
      "libpng",
      "libvulkan",
      "libz",
  ],
  static_libs: [
      "cpufeatures",  // TODO: use this for CRC32 detection
      "libsfntly",
      "libwebp-decode",
      "libwebp-encode",
  ],
''')

# TODO: run GN --ide=json, get srcs, export_includes, local_includes from that.
# TODO: create SkUserConfig.h

# For architecture specific files, it's easier to just read the same source
# that GN does (opts.gni) rather than re-run GN once for each architecture.

# This .gni file we want to read is close enough to Python syntax
# that we can use execfile() if we supply definitions for GN builtins.

def get_path_info(path, kind):
  assert kind == "abspath"
  # While we want absolute paths in GN, relative paths work best here.
  return path

builtins = {
  'get_path_info': get_path_info,
}
definitions = {}
execfile(os.path.join(os.path.dirname(sys.argv[0]), 'opts.gni'),
         builtins, definitions)

def scrub(srcs):
  # Perform any string substitutions.
  for var in definitions:
    if type(definitions[var]) is str:
      srcs = [ p.replace('$'+var, definitions[var]) for p in srcs ]
  # Relativize paths to top-level skia/ directory.
  return [os.path.relpath(p, '..') for p in srcs]

def to_str(lst):
  return pprint.pformat(lst).replace("'", '"')

print t.substitute({
  'export_includes': to_str([]),
  'local_includes':  to_str([]),
  'srcs':            to_str([]),

  'arm_srcs':      to_str(scrub(definitions['armv7'])),
  'arm_neon_srcs': to_str(scrub(definitions['neon'])),
  'arm64_srcs':    to_str(scrub(definitions['arm64'] +
                                definitions['crc32'])),
  'mips_srcs':     to_str(scrub(definitions['mips_dsp'])),
  'mips64_srcs':   to_str(scrub(definitions['none'])),
  'x86_srcs':      to_str(scrub(definitions['sse2'] +
                                definitions['ssse3'] +
                                definitions['sse41'] +
                                definitions['sse42'] +
                                definitions['avx'  ] +
                                definitions['hsw'  ]))
})
