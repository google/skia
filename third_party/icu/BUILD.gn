# Copyright 2016 Google Inc.
#
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

declare_args() {
  skia_use_system_icu = is_official_build
}

import("../third_party.gni")
import("icu.gni")

if (skia_use_system_icu) {
  system("icu") {
    libs = [ "icuuc" ]
    defines = [ "U_USING_ICU_NAMESPACE=0" ]
  }
} else {
  _data_assembly = "$target_gen_dir/icudtl_dat.S"
  third_party("icu") {
    public_include_dirs = [
      "../externals/icu/icu4c/source/common",
      "../externals/icu/icu4c/source/i18n",
      ".",
    ]
    public_defines = [ "U_USING_ICU_NAMESPACE=0" ]
    defines = [
      # http://userguide.icu-project.org/howtouseicu
      "U_ENABLE_DYLOAD=0",
      "U_NOEXCEPT=",
      "U_STATIC_IMPLEMENTATION",
      "U_COMMON_IMPLEMENTATION" ,
      "U_I18N_IMPLEMENTATION",
    ]
    configs -= [ "//gn:no_rtti" ]
    sources = icu_sources + [ _data_assembly ]
    deps = [ ":assemble_data" ]
  }
  action("assemble_data") {
    script = "make_data_assembly.py"
    sources = [ icu_dat_path ]
    outputs = [ _data_assembly ]
    args = [
      icu_dtname,
      current_os,
      target_cpu,
      rebase_path(icu_dat_path, root_build_dir),
      rebase_path(_data_assembly, root_build_dir),
    ]
  }
}
