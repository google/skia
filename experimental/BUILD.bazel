# This empty BUILD file makes 'experimental' a Bazel package, allowing
# '//experimental/...' queries to work even when only a subset of subdirectories
# are present (e.g., during Gerrit per-file analysis).
